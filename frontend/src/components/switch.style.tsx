import styled from "styled-components"

export const SwitchStyle=styled.div`
    position:absolute;
    top: 2em;
    right:3em;
    & .material-icons{
        position:relative;
        top:8px;
    }
`